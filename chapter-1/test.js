var assert = require('assert')

describe('Example Test File', function() {
  it('Should Pass', function() {
    assert(true)
  })

  it('Should Fail', function() {
    assert(false, 'It Failed!')
  })
})
